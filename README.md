# Zero-shot land cover distribution estimation

This project contributes to my research in analyzing Earth Observation Foundation Models to connect them together with other modality models, to create interpretable downstream tasks (e.g. creating a zero-shot crop type distribution generator).
